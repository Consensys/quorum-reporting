package quorum_reporting

// hardcoded parameters should be converted into flags after integrating with github.com/urfave/cli/v2
var (
	quorumWSurl = "ws://localhost:23000"
	quorumGraphQLurl = "http://localhost:8547/graphql"
)

func main() {

}